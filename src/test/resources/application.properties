# Test-only configuration — NOT deployed to production.
# Provides placeholder values for environment variables required at startup.
# Real values are always set via environment variables in production (see .env.example).
ADMIN_API_KEY=test-admin-key
